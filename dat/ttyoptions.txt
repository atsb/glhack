
    glHack version 1.3

    NetHack version 3.4.3

Options compiled into this edition:

    patch: auto open doors-patchable, color, data librarian, debug mode,
    Elbereth, experience points on status line,
    insurance files for recovering from crashes, Keystone Kops,
    exclusive lock on level 0 file, log file, redo command, rogue level,
    saddles and riding, score on status line, screen clipping, seduction,
    sinks, tourists, user sounds via pmatch, walled mazes,
    zero-compressed save files,
    save and bones files accepted from versions 3.4.0 through 3.4.3,
    basic NetHack features.

Supported windowing systems:

    GL,
    with a default of GL.

